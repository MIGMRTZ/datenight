---
id: T2.5
title: CLI Couple Commands
plan: plan-2026-03-sprint2-profiles-couples
status: done
priority: P0
complexity: 30
completed_at: '2026-03-31T17:31:07.692892'
---

## Objective
Implement 3 couple CLI subcommands for linking/viewing/unlinking couples.

## Acceptance Criteria
- [x] `datenight couple create` lists profiles, links two as a couple
- [x] `datenight couple create` errors if fewer than 2 profiles
- [x] `datenight couple show` displays couple with both profiles
- [x] `datenight couple show` handles zero/multiple couples
- [x] `datenight couple unlink` removes couple link with confirmation
- [x] 6 tests pass (CliRunner + mocked client)

## Implementation Details
- File: `datenight/commands/couple.py`
- couple_app = typer.Typer() with 3 commands
- MVP: assume one couple. Zero → prompt to create. Multiple → list and pick.
- Registered in cli.py via app.add_typer()

## Dependencies
- T2.3 (API client)
- T2.4 (profile commands — shares patterns)