---
id: T1.5
title: D1 Initial Migration
plan: plan-2026-03-sprint1-foundation
status: done
priority: P0
complexity: 20
completed_at: '2026-03-31T15:51:00.029198'
---

## Objective
Create the D1 migration with partners, couples, and date_history tables. Add PRAGMA foreign_keys = ON to Worker request handling.

## Acceptance Criteria
- [x] `worker/migrations/0001_initial.sql` creates all 3 tables + 3 indexes
- [x] `worker/src/db/schema.sql` reference copy exists
- [x] UNIQUE constraints on couples.partner_a and couples.partner_b
- [x] CHECK constraint on rating (1-5)
- [x] Worker runs `PRAGMA foreign_keys = ON` per-request
- [x] Migration applies locally: `npx wrangler d1 migrations apply datenight --local`
- [x] Tests verify tables and indexes exist

## Implementation Details

**Tables:** partners, couples, date_history (exact SQL from spec)

**Key D1 notes:**
- No DROP COLUMN / ALTER COLUMN
- JSON stored as TEXT, queryable via json_extract()
- Foreign keys need PRAGMA per-connection

**Tests:**
- Tables exist (SELECT from sqlite_master)
- Indexes exist
- Insert/read round-trip on partners table

## Dependencies
- T1.3 (Worker skeleton with wrangler.toml)