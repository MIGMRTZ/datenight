---
id: T1.9
title: Example Config Files
plan: plan-2026-03-sprint1-foundation
status: pending
priority: P2
complexity: 10
---

## Objective
Create documentation files for all required env vars and config keys.

## Acceptance Criteria
- [ ] `.env.example` documents DATENIGHT_AUTH_TOKEN and optional overrides
- [ ] `config.example.yaml` has full config with inline comments
- [ ] Every key in DateNightSettings has an entry in config.example.yaml
- [ ] Neither file contains real secrets

## Implementation Details

**.env.example:**
```
DATENIGHT_AUTH_TOKEN=your-secret-token-here
# DATENIGHT_LOCATION__ZIP=75165
# DATENIGHT_CLOUDFLARE__WORKER_URL=https://...
# DATENIGHT_LOGGING__LEVEL=DEBUG
```

**config.example.yaml:** Full config shape from spec with comments explaining each field.

No code tests — manual verification only.

## Dependencies
- T1.2 (config management — need to know all keys)
