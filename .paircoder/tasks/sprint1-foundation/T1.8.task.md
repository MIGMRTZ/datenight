---
id: T1.8
title: CLI Entry Point
plan: plan-2026-03-sprint1-foundation
status: pending
priority: P0
complexity: 35
---

## Objective
Create the Typer CLI app with `--version` flag, help text, and config/logging initialization.

## Acceptance Criteria
- [ ] `datenight/cli.py` has Typer app with `--version`/`-V` flag
- [ ] `no_args_is_help=True` — bare `datenight` shows help
- [ ] Callback loads settings and sets up logging
- [ ] `datenight --version` prints `datenight 0.1.0`
- [ ] 5 tests pass

## Implementation Details

**CLI structure:**
- `app = typer.Typer(name="datenight", no_args_is_help=True)`
- `version_callback` as eager option
- `main()` callback loads settings + setup_logging

**Tests (using typer.testing.CliRunner):**
- test_version_flag (--version)
- test_version_short_flag (-V)
- test_no_args_shows_help
- test_help_flag (--help)
- test_unknown_command_errors

## Dependencies
- T1.1 (project scaffolding)
- T1.2 (config management)
- T1.7 (structured logging)
